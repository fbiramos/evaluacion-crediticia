{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "🚀 Desplegar a GitHub",
            "type": "shell",
            "command": "powershell -Command \"(Get-Content sw.js) -replace 'v\\d+', ('v' + (Get-Date -Format 'MMddHHmm')) | Set-Content sw.js; git add .; git commit -m ('🚀 Deploy ' + (Get-Date -Format 'HH:mm')); git push origin main; firebase deploy\"",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}